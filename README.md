# website-tddigital
Portal Website Tekom.Dev Digital

## Development

```bash
npm install
npm run dev
```

Create a production bundle with `npm run build`. The Vite base path is
configured for GitHub Pages, and `CNAME` is copied into the generated `dist`
directory.
